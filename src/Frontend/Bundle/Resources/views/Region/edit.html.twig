{% extends 'FrontendBundle:Layouts:base.html.twig' %}

{% block body -%}
    <h1>Regiones</h1>
    <br/>
    {{ form(edit_form) }}
    <a href="{{ path('region') }}">Regresar</a>
    <br/>
    {{ render(controller('FrontendBundle:Region:index')) }}
{% endblock %}
