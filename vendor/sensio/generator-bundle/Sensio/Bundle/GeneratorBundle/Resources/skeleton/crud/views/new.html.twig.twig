{% block extends %}
{{ "{% extends 'FrontendBundle:Layouts:base.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block body -%}" }}
    <h1>{{ entity }}s</h1>
    <br/>
    {{ '{{ form(form) }}' }}
    &nbsp;
    <br/>
    {{ "{{ render(controller('FrontendBundle:"}}{{ entity }}{{ ":index')) }}" }}
{{ "{% endblock %}" }}
{% endblock body %}